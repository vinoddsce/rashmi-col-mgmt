export default class Student {

  private name: string;
  private course: string;
  private fees: number;

  constructor(name: string, course: string, fees: number) {
    this.name = name;
    this.course = course;
    this.fees = fees;
  }

}
